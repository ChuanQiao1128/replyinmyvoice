TASK: Add an admin "delete (erase) user" backend endpoint — DELETE console/users/{userId} — that anonymizes the account by reusing the proven AccountService erase, with guards and an audit-log entry.

CONTEXT
- Repo: /Users/qc/Desktop/CloudFlare. Branch feat/admin-polish (off current main). Spec: plans/admin-polish-plan.md.
- Backend is C#/.NET 8 Azure Functions (isolated worker) + EF Core + Azure SQL. Solution: `backend-dotnet/ReplyInMyVoice.sln`.
- Admin HTTP endpoints: `backend-dotnet/src/ReplyInMyVoice.Functions/Functions/AdminHttpFunctions.cs`. IMPORTANT: admin routes use the `console/` prefix (NOT `admin/`, which is reserved by the Functions host). Existing user endpoints: `GET console/users`, `GET console/users/{userId}`, `POST console/users/{userId}/credits`, `POST console/users/{userId}/suspension`, `POST console/users/{userId}/refund`.
- The mutation handlers follow a fixed shape (mirror `AdminSetUserSuspension`, ~line 517): call `var admin = await _adminAccess.RequireAdminAsync(request, ct);` → if null return 403 Problem; if `_adminService is null` return `AdminServiceUnavailable()`; `Guid.TryParse(userId, out var parsedUserId)` else 400 Problem; call the service; `switch (result.Kind)` → map to `OkObjectResult` / 404 / 400 / 403 Problem via `FunctionHttpResults.Problem(title, detail, statusCode)`. `RequireAdminAsync` returns an object with `.ExternalAuthUserId` and `.Email`.
- Service: `backend-dotnet/src/ReplyInMyVoice.Infrastructure/Services/AdminService.cs` (`public sealed class AdminService(Func<AppDbContext> dbContextFactory, IStripeRefundClient? stripeRefundClient = null, TaxTurnoverService? taxTurnoverService = null)`). It writes audit rows via `db.AdminAuditLogs.Add(new AdminAuditLog { … })` (see `SetUserSuspensionAsync` ~line 556 and `ResolveBillingSupportRequestAsync` for the AdminAuditLog shape/fields to copy).
- The proven erase already exists: `AccountService.DeleteAccountAsync(string externalAuthUserId, CancellationToken)` (backend-dotnet/src/ReplyInMyVoice.Infrastructure/Services/AccountService.cs, ~line 214) — it cancels any Stripe subscription, then in a transaction anonymizes the AppUser (`ExternalAuthUserId = CreateErasedExternalAuthUserId(user.Id)` i.e. `erased:{id}`, `Email = null`, `SubscriptionStatus = Canceled`) and scrubs all child rows (RewriteAttempts, UsagePeriods, UsageReservations, RewriteCredits, PromoCodeRedemptions, BillingSupportRequests). It is idempotent (no-ops if the user is missing or already erased). `IsErasedExternalAuthUserId(s)` returns `s.StartsWith("erased:")`.

CHANGES REQUIRED
1. **Service** — add `AdminService.DeleteUserAsync(string adminExternalAuthUserId, string? adminEmail, Guid userId, DateTimeOffset now, CancellationToken ct)` returning a small result type (e.g. `AdminDeleteUserServiceResult` with a `Kind` enum: `Success`, `UserNotFound`, `Forbidden`, plus an optional `Detail` and a `Response` payload object for success). Logic:
   - Load the AppUser by `Id == userId` (AsNoTracking is fine for the lookup). If not found → `UserNotFound`.
   - If the target is already erased (`IsErasedExternalAuthUserId(user.ExternalAuthUserId)`) → `Forbidden` with detail "account already erased" (idempotent-safe; nothing to do).
   - If the target is the acting admin themselves (`user.ExternalAuthUserId == adminExternalAuthUserId`) → `Forbidden` with detail "an admin cannot delete their own account from the console".
   - Otherwise: capture the target's `ExternalAuthUserId`, perform the erase by REUSING the existing `AccountService.DeleteAccountAsync(targetExternalId, ct)` (preferred — do not duplicate the 8-entity scrub). Wire `AccountService` into `AdminService` however is cleanest (e.g. inject it via the ctor and register it in the DI module where `AdminService` is constructed — search the Functions `Program.cs` / DI setup for `new AdminService(` or `AddSingleton/AddScoped<AdminService>`; `AccountService` is already DI-registered). Keep the `AdminService` ctor's existing optional params and tests compiling.
   - Write an `AdminAuditLog` row (action e.g. `"user.delete"`, actor = adminExternalAuthUserId/adminEmail, target = userId, timestamp = now), matching the existing AdminAuditLog field shape used by `SetUserSuspensionAsync`.
   - Return `Success` with a small response (e.g. `{ userId, status = "erased" }`).
2. **Endpoint** — add `[Function("AdminDeleteUser")]` handling `[HttpTrigger(AuthorizationLevel.Anonymous, "delete", Route = "console/users/{userId}")]` in `AdminHttpFunctions.cs`, mirroring `AdminSetUserSuspension`'s admin-guard + `_adminService` null-check + `Guid.TryParse` + result switch. Map `Success`→`OkObjectResult(result.Response)`, `UserNotFound`→404, `Forbidden`→403, default→400, all via `FunctionHttpResults.Problem`. (The existing `GET console/users/{userId}` and the new `DELETE console/users/{userId}` coexist — different HTTP methods on the same route are fine.)
3. **Tests** — add coverage in the backend test project (mirror existing admin function/service tests, e.g. the suspension tests): DeleteUserAsync erases a normal user (afterwards the user is anonymized / `IsErasedExternalAuthUserId` true) + writes an audit row; returns `UserNotFound` for a missing id; returns `Forbidden` for self-delete and for an already-erased target. Keep all existing tests green.

ACCEPTANCE (machine-checkable)
- `grep -rn 'console/users/{userId}"' backend-dotnet/src` shows both a `"get"` and a `"delete"` trigger; `grep -rn 'AdminDeleteUser' backend-dotnet/src` resolves.
- `dotnet test backend-dotnet/ReplyInMyVoice.sln` green (new + existing).
- No new EF migration (DeleteAccountAsync + AdminAuditLog already exist). No banned terms. No new secrets.
- (Post-deploy, manual) `curl -X DELETE $FUNC/api/console/users/<guid>` without an admin token returns 401/403, not 404/500.

DO NOT
- Do not hard-delete rows or add cascade deletes — reuse the anonymize path (`DeleteAccountAsync`). Do not change `DeleteAccountAsync`, `RequireAdminAsync`, ADMIN_EMAILS, the `console/` prefix, or other admin endpoints. Do not add a migration. No banned terms (humanizer|bypass|undetect|detector|evade). Do not push/PR/merge/deploy — the driver handles git.
