## Context

- Repo root: `/Users/qc/Desktop/CloudFlare`; backend at `/Users/qc/Desktop/CloudFlare/backend-dotnet`. Base branch = `delivery/backend-hardening-2` (NEVER `main`).
- Finding #23: config is read via `configuration[...]` index lookups with zero validated-Options usage; bad values are silently clamped/defaulted; no in-app Key Vault. See `plans/backend-hardening-2/SPEC.md` section **OBS-OPTIONS**.
- The rewrite-engine knobs are read inline in `backend-dotnet/src/ReplyInMyVoice.Infrastructure/ServiceCollectionExtensions.cs:304-336`: `OPENAI_BASE_URL`, `OPENAI_MODEL_MID_WRITER`/`OPENAI_MODEL`, `OPENAI_TIMEOUT_SEC` (default 60), `WRITING_SIGNAL_TIMEOUT_SEC` (default 10), `AI_SIGNAL_TARGET` (default 20), `REWRITE_MAX_ATTEMPTS` (default 10), `TOTAL_REWRITE_BUDGET_SEC` (default `DefaultTotalRewriteBudgetSeconds=180`, line 47). Each uses `int.TryParse(...)` and silently falls back on bad input.
- Existing fail-fast precedent: `ValidateReplyInMyVoiceRuntimeConfiguration` (`ServiceCollectionExtensions.cs:503-570`) throws `InvalidOperationException` in non-dev environments. `IsDevelopmentOrTesting` (line 579) returns true for `Development`/`Testing`.
- Existing options-test harness: `backend-dotnet/tests/ReplyInMyVoice.Tests/InfrastructureServiceCollectionTests.cs` — `BuildProvider(values, environmentName="Testing", requireServiceBusConsumer=false)` (line 684) and the clamp tests at lines 213-233 and 629-654 are the templates to copy.
- IMPORTANT environment fact (verified): `Microsoft.Extensions.Options` 10.0.8 IS in the local nuget cache and exposes `AddOptions<T>()`, `.Bind()`, `.Validate(...)`, `.ValidateOnStart()`. The `Microsoft.Extensions.Options.DataAnnotations` package (the `.ValidateDataAnnotations()` extension) and the Azure Key Vault packages are NOT cached. The worktree may be offline — adding a non-cached PackageReference risks a restore failure.

## Constraints

- Banned terms (CI grep guard, halt on match): `humanizer | bypass | undetect | detector | evade` — never as a substring in any new file/identifier/comment.
- Do NOT change `IRewriteEngineClient` / engine result JSON / error-code set (frozen black box). Do NOT touch `FactReconstructRewriteProvider`, `FactReconstructRewriteOptions`, `RewriteProviderEngineClient`, or the provider-selection branches (lines 338-379) other than reading the knob values from the bound options object instead of re-reading raw config.
- Keep the existing 799 tests green; ADD tests. `Development`/`Testing` must NOT fail-fast on bad/missing values (mirror `IsDevelopmentOrTesting`) so the whole existing suite (which runs as `Testing`) stays green.
- Prefer the already-cached `Microsoft.Extensions.Options` — implement DataAnnotations-style validation via a custom `IValidateOptions<RewriteEngineOptions>` (or an `.Validate(...)` predicate) so NO new package is required. Do NOT add `Microsoft.Extensions.Options.DataAnnotations`.
- Key Vault must be **opt-in and guarded**: only attach when an explicit setting (e.g. `KEY_VAULT_URI` / `AZURE_KEY_VAULT_URI`) is present AND managed identity is enabled; wrap in try/catch so a missing package / offline / unreachable vault NEVER crashes boot or the tests. If wiring the real `Azure.Extensions.AspNetCore.Configuration.Secrets` source would require a non-cached PackageReference that can't restore offline, implement the extension method as a no-op-when-unconfigured seam that is unit-testable WITHOUT the live Azure SDK (assert the guard logic), and leave a clearly-commented TODO for the package add — do not break the build to ship the SDK call.
- No secret values in tracked files; validate env at runtime in the composition root, not at type-import.

## Changes required

1. New `backend-dotnet/src/ReplyInMyVoice.Infrastructure/Configuration/RewriteEngineOptions.cs`: a class (mutable properties so `.Bind()` works) with `ModelBaseUrl` (string, default `https://api.openai.com/v1`), `Model` (string, default `gpt-4o-mini`), `ModelTimeoutSeconds` (int, default 60), `SignalTimeoutSeconds` (int, default 10), `AiSignalTarget` (int, default 20), `MaxAttempts` (int, default 10), `TotalBudgetSeconds` (int, default 180, where 0 == unlimited). Include a `const string SectionName` and document valid ranges in XML comments. Provide a config-key map so existing env names still bind (e.g. via per-property `.Bind` calls or a custom binder reading `OPENAI_TIMEOUT_SEC` etc.) — existing env var names MUST keep working.
2. New `backend-dotnet/src/ReplyInMyVoice.Infrastructure/Configuration/RewriteEngineOptionsValidator.cs`: `IValidateOptions<RewriteEngineOptions>` returning `ValidateOptionsResult.Fail(...)` with a message naming the offending setting when: `ModelTimeoutSeconds < 1`, `SignalTimeoutSeconds < 1`, `AiSignalTarget < 0 or > 100`, `MaxAttempts < 1 or > 50`, `TotalBudgetSeconds < 0`, or `ModelBaseUrl` not an absolute http(s) URI. Use the existing env key names in the failure message so operators can act on it.
3. In `ServiceCollectionExtensions.cs`: register `services.AddOptions<RewriteEngineOptions>().Bind(...).ValidateOnStart();` and `services.AddSingleton<IValidateOptions<RewriteEngineOptions>, RewriteEngineOptionsValidator>();`. Gate `.ValidateOnStart()` strictness so it only hard-fails outside Development/Testing (reuse the existing `ResolveRuntimeEnvironmentName`/`IsDevelopmentOrTesting` helpers), OR keep `ValidateOnStart` always-on but ensure the existing `Testing` suite supplies/falls-back to valid defaults (defaults already satisfy the validator, so unset values stay valid — only explicitly-bad values fail). Then change the inline reads at lines 304-336 to source their integer/string values from a resolved `RewriteEngineOptions` instance (build it once from the bound section before the provider branch) so there is a single parse path. Behavior for valid/absent values MUST be identical to today (defaults 60/10/20/10/180, base-url fallback, model fallback).
4. New `backend-dotnet/src/ReplyInMyVoice.Infrastructure/Configuration/KeyVaultConfigurationExtensions.cs`: `public static IConfigurationBuilder AddReplyInMyVoiceKeyVault(this IConfigurationBuilder builder, IConfiguration configuration)` that reads `KEY_VAULT_URI`/`AZURE_KEY_VAULT_URI`, returns the builder unchanged when unset or when managed identity is disabled, and only attaches the secret source (using `DefaultAzureCredential`, continuing HARD-10) when configured — all inside try/catch so failures log-and-continue, never throw. Make the guard decision (`ShouldAttach`/`ResolveVaultUri`) a separately testable static method.
5. In `Functions/Program.cs`: call `builder.Configuration.AddReplyInMyVoiceKeyVault(builder.Configuration);` immediately after the existing `AddJsonFile(version.generated.json)` block (line 18) and before `AddReplyInMyVoiceInfrastructure`. It must be a no-op in local/test (no `KEY_VAULT_URI`).
6. Only edit `ReplyInMyVoice.Infrastructure.csproj` if a genuinely-required, cached package is needed. Do NOT add a package that cannot restore offline; if the Azure secrets SDK is unavailable, ship the guard seam (point 4) without the live SDK call and leave a TODO comment.
7. New `backend-dotnet/tests/ReplyInMyVoice.Tests/RewriteEngineOptionsTests.cs` (xUnit + FluentAssertions, copy the harness from `InfrastructureServiceCollectionTests`): (a) valid/default config → options resolve with expected defaults and provider DI still builds; (b) a bad value (`REWRITE_MAX_ATTEMPTS=abc` or `OPENAI_TIMEOUT_SEC=-1`) under a non-dev environment name → `BuildProvider(... environmentName:"Production")` (or resolving `IOptions<RewriteEngineOptions>.Value`) throws `OptionsValidationException`/`InvalidOperationException` whose message names the offending key; (c) the same bad value under `Testing`/`Development` does NOT hard-fail (preserves the existing suite); (d) `KeyVaultConfigurationExtensions` guard returns the builder unchanged when `KEY_VAULT_URI` is unset; (e) a unit test asserting no secret string is leaked into the thrown message.

## Acceptance

- `cd backend-dotnet && dotnet test ReplyInMyVoice.sln -c Release --filter FullyQualifiedName~RewriteEngineOptionsTests`
- `cd backend-dotnet && dotnet test ReplyInMyVoice.sln -c Release --filter FullyQualifiedName~InfrastructureServiceCollectionTests`
- `cd backend-dotnet && dotnet test ReplyInMyVoice.sln -c Release`
- `cd backend-dotnet && grep -q "AddOptions<RewriteEngineOptions>" src/ReplyInMyVoice.Infrastructure/ServiceCollectionExtensions.cs && grep -q "ValidateOnStart" src/ReplyInMyVoice.Infrastructure/ServiceCollectionExtensions.cs`
- `cd backend-dotnet && grep -q "AddReplyInMyVoiceKeyVault" src/ReplyInMyVoice.Functions/Program.cs`
- `cd backend-dotnet && ! grep -RniE "humanizer|bypass|undetect|detector|evade" src/ReplyInMyVoice.Infrastructure/Configuration src/ReplyInMyVoice.Functions/Program.cs tests/ReplyInMyVoice.Tests/RewriteEngineOptionsTests.cs`

## DO NOT

- Do NOT change the `IRewriteEngineClient` contract, the engine result JSON / error-code set, or `FactReconstructRewriteOptions`/`FactReconstructRewriteProvider`/`RewriteProviderEngineClient`.
- Do NOT migrate the other ~111 `configuration[...]` lookups or alter the existing `ProviderCircuitBreakerOptions`/`StripeEventProcessingOptions`/`StripeReconciliationOptions` records or `ReadClampedInt`/`ReadBoundedInt`.
- Do NOT add a non-cached PackageReference that breaks an offline restore (`Microsoft.Extensions.Options.DataAnnotations`, Azure Key Vault SDK) — use the cached `Microsoft.Extensions.Options` + custom validator; ship Key Vault as a guarded seam if the SDK can't restore.
- Do NOT make Key Vault mandatory or let it throw at boot; do NOT make Development/Testing fail-fast (would break the existing suite).
- Do NOT introduce banned terms (`humanizer|bypass|undetect|detector|evade`); do NOT put secret values in tracked files.
- The worker must NEVER push, open a PR, or touch `main` — commit to the issue branch off `delivery/backend-hardening-2` only.