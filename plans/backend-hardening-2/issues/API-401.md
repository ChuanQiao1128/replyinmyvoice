## Context

Repo root: `/Users/qc/Desktop/CloudFlare`. Backend: .NET 8 / Azure Functions under `backend-dotnet`. Integration branch base = `delivery/backend-hardening-2` (NEVER `main`). Wave spec: `plans/backend-hardening-2/SPEC.md` → section **API-401** (finding #17). Solution has 799 passing tests; keep green and ADD tests. Test base: `cd backend-dotnet && dotnet test ReplyInMyVoice.sln -c Release`.

Read first (ground every change in these exact anchors):
- `backend-dotnet/src/ReplyInMyVoice.Functions/Auth/FunctionAuthResolver.cs` — `ResolveUserAsync` (lines 17-53) returns `null` for ALL bearer failure modes: no-token (38-41), metadata fetch fail (`catch { return null; }` 142-145), expired/bad-sig/bad-aud/missing-scope (`catch { return null; }` 159-171). `ResolveBearerToken` (107-116). `ValidateBearerTokenAsync` (118-172) is where the `SecurityTokenExpiredException` vs other `SecurityTokenException` distinction is available.
- `backend-dotnet/src/ReplyInMyVoice.Functions/Http/FunctionHttpResults.cs` — `Problem(...)` (8-41) builds the coded envelope; `DefaultErrorCode` maps 401→`"unauthorized"` (57). No way to set headers today; no `WWW-Authenticate` anywhere in `src` (grep-confirmed).
- `backend-dotnet/src/ReplyInMyVoice.Functions/Functions/RewriteHttpFunctions.cs` — primary-ctor class (20-27, has `IConfiguration configuration`); 5 bearer-auth 401 sites (`ResolveUserAsync` + `Problem(..., Status401Unauthorized)` at 40-47, 146-152, 180-186, 256-262, 299-305). Wire only this consumer.
- `backend-dotnet/tests/ReplyInMyVoice.Tests/FunctionAuthResolverTests.cs` — existing 12 tests build `DefaultHttpContext` requests + in-memory config; no mock framework. Match this style.

## Constraints

- Banned substrings anywhere (CI grep, halt on match): `humanizer | bypass | undetect | detector | evade`.
- Do NOT change `IRewriteEngineClient` / rewrite engine contract or its error-code set (frozen black box).
- Do NOT alter the JWT `TokenValidationParameters` / validation semantics (TEST-JWT owns real-JWT negative paths) — only CLASSIFY the existing thrown exception/return into NoToken / Expired / Invalid.
- Do NOT touch the V1 API-key auth path (`V1RewriteHttpFunctions.cs`, `ApiKeyAuthResolver.cs`) — that uses `invalid_key`, a separate taxonomy.
- No secret values / no token contents / no PII (email, raw token, claims) in any log line — log only the subtype enum + a generic message.
- No new mocking/FakeLogger NuGet dependency (project references none) — use a small inline capturing `ILogger<RewriteHttpFunctions>` in the test.
- ≤8 files; keep `ResolveUserAsync` and all 12 existing `FunctionAuthResolverTests` green (the other consumers — Account/ApiKey/Billing/Promo/ApiUsage — keep calling `ResolveUserAsync` unchanged).

## Changes required

1. `FunctionAuthResolver.cs`: add a 401-subtype enum (e.g. `public enum AuthFailureReason { None, NoToken, Expired, Invalid }`) and a result record (e.g. `public sealed record FunctionAuthResult(FunctionAuthUser? User, AuthFailureReason Reason)`). Add `ResolveUserResultAsync(...)` that mirrors `ResolveUserAsync` but returns the result:
   - header/principal identity resolved → `Reason = None`, user set.
   - bearer token absent (`ResolveBearerToken` null/whitespace) → `NoToken`.
   - in `ValidateBearerTokenAsync`, distinguish `catch (SecurityTokenExpiredException)` → `Expired` from any other failure (metadata fetch fail, `SecurityTokenException`, missing scope, empty user id) → `Invalid`. Surface this up (e.g. have `ValidateBearerTokenAsync` return a small tuple/result, or split into an outcome).
   - Keep `public ResolveUserAsync` as a thin wrapper returning `result.User` so existing callers + tests are unchanged.
2. `FunctionHttpResults.cs`: add `public static IActionResult Unauthorized(string detail, bool invalidToken)` returning the existing coded 401 envelope (`code = "unauthorized"`) AND setting the response header in the result: `WWW-Authenticate: Bearer error="invalid_token"` when `invalidToken` is true, else bare `WWW-Authenticate: Bearer`. Implement header-setting via a custom `IActionResult`/`ObjectResult` subtype (or wrap in a result that sets `context.HttpContext.Response.Headers.WWWAuthenticate`) since `Problem` cannot set headers.
3. `RewriteHttpFunctions.cs`: inject `ILogger<RewriteHttpFunctions>` into the primary ctor (add `Microsoft.Extensions.Logging`). At each of the 5 bearer-auth sites, call `ResolveUserResultAsync`; on failure, log the subtype once (`logger.LogInformation("auth.unauthorized subtype={Reason}", reason)` — NO token/email) and return `FunctionHttpResults.Unauthorized(detail, invalidToken: reason != AuthFailureReason.NoToken)`. `NoToken` → bare `Bearer`; `Expired`/`Invalid` → `error="invalid_token"`.
4. `FunctionAuthResolverTests.cs`: add tests — (a) no bearer header → `ResolveUserResultAsync` returns `Reason == NoToken`; (b) a request whose bearer validation fails for a non-expiry reason (e.g. authority/audience unconfigured so `ValidateBearerTokenAsync` returns the invalid path) → `Reason == Invalid`; (c) the expiry path maps to `Expired` (drive it through whatever seam you expose — a direct classifier method `ClassifyTokenFailure(Exception)` is acceptable and unit-testable without signing a real JWT); (d) `FunctionHttpResults.Unauthorized(..., invalidToken:true)` executed against a `DefaultHttpContext` sets `WWW-Authenticate: Bearer error="invalid_token"` and status 401; `invalidToken:false` sets bare `Bearer`; (e) a capturing `ILogger` records the subtype with no token/email substring.

## Acceptance

- `cd backend-dotnet && dotnet test ReplyInMyVoice.sln -c Release --filter FullyQualifiedName~FunctionAuthResolverTests`
- `cd backend-dotnet && dotnet test ReplyInMyVoice.sln -c Release`
- `grep -RniE "humanizer|bypass|undetect|detector|evade" backend-dotnet/src/ReplyInMyVoice.Functions/Auth backend-dotnet/src/ReplyInMyVoice.Functions/Http backend-dotnet/src/ReplyInMyVoice.Functions/Functions/RewriteHttpFunctions.cs backend-dotnet/tests/ReplyInMyVoice.Tests/FunctionAuthResolverTests.cs; test $? -eq 1`
- `grep -RnE 'error="invalid_token"' backend-dotnet/src/ReplyInMyVoice.Functions/Http/FunctionHttpResults.cs`

## DO NOT

- Do NOT change `IRewriteEngineClient` / rewrite engine contract / its error codes.
- Do NOT change JWT validation semantics or the V1 API-key auth path (`V1RewriteHttpFunctions.cs`, `ApiKeyAuthResolver.cs`).
- Do NOT log token contents, claims, email, or any PII; do NOT print/commit secrets.
- Do NOT add a mocking/FakeLogger NuGet package.
- Do NOT touch files outside the 4 scope paths; do NOT break the 12 existing `FunctionAuthResolverTests` or any other consumer of `ResolveUserAsync`.
- Do NOT push, open a PR, or touch `main`; commit only to a branch off `delivery/backend-hardening-2`.