# M0-001: Audit dirty working tree → triage report

**Milestone**: M0-Stabilize
**Est**: ≤30 min
**Type**: read-only audit, produces a planning doc

## TASK
Inventory the 52 uncommitted modified files in `/Users/qc/Desktop/CloudFlare` and produce a triage report classifying each as **keep / discard / stash**.

## CONTEXT
- Repo root: `/Users/qc/Desktop/CloudFlare`
- `git status --short` shows ~52 files modified, mostly in two clusters: (a) auth migration WIP (Clerk → Entra), (b) rewrite gate calibration WIP
- Recent commits on `main`: Entra Google login, fact ledger, adaptive gate calibration, OAuth fixes
- Stabilizing this tree is M0 prerequisite for the entire commercialization roadmap (`plans/commercialization-roadmap.md`)

## CONSTRAINTS
- **Read-only**: do NOT commit, push, edit source files, or run `git stash` in this issue
- Banned terms scan still applies if you create new files: `humanizer | bypass | undetect | detector | evade`
- Do not print secrets from `.env.local`, `.dev.vars`, `globalapikey/`

## CHANGES REQUIRED
1. Run `git status --short` and `git diff --stat` to list every modified file with line-count change
2. For each file, examine the diff (use `git diff <file>` per file as needed) and decide:
   - **keep**: change is purposeful WIP that should land on main (auth migration, quality fixes, doc updates)
   - **discard**: stale/abandoned experiment or accidental edit
   - **stash**: unfinished but valuable — save for later, not now
3. Write `plans/wip-triage.md` with this structure:
   ```
   # WIP Triage — 2026-05-21

   ## Keep (themed commits in M0-002)
   ### Theme: <e.g. Entra migration>
   - path/to/file (+12 -3): one-line rationale
   ...

   ## Discard
   - path/to/file: why

   ## Stash
   - path/to/file: why; restore criteria
   ```
4. At top, summary line: total kept / discarded / stashed counts

## ACCEPTANCE
- `plans/wip-triage.md` exists and lists all 52 (or actual count) modified files
- Every file appears exactly once with a decision
- "Keep" entries are grouped by theme suitable for 2-4 themed commits
- No files committed, pushed, or modified by this issue

## DO NOT
- Run `git add`, `git commit`, `git push`, `git stash`, or `git restore`
- Edit any source file
- Touch `LAUNCH_CONFIRMED`, Stripe config, or any secret
- Open PRs

## On completion
Comment on this GitHub issue with the summary line (`Kept N, discarded M, stashed P`) and close it.
