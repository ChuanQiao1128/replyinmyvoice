# Issue M3-005

Title: M3-005 Enforce 5000-char combined cap in lib/validation.ts
Milestone: M3-V2

## Task

Enforce the confirmed per-field and combined character limits in the rewrite request validation schema.

## Scope

- Touch only `lib/validation.ts` and `tests/unit/validation.test.ts`.
- Set max lengths: `messageToReplyTo` 3000, `roughDraftReply` 3000 with minimum 10, `audience` 300, `purpose` 500, `context` / what happened 1000, and facts to preserve 1000.
- Enforce a 5000-character combined cap across submitted fields, including accepted legacy fields.

## Constraints

- Do not modify `.env.local`, `.dev.vars`, secrets, live provider settings, or Stripe configuration.
- Follow the current banned-positioning list from `plans/codex-implementation-prompt.md`; do not quote the blocked words in new copy.

## Verification

- `npm run lint`
- `npm run typecheck`
- `npm run test`
