# M0-004: Green baseline — lint, typecheck, tests pass

**Milestone**: M0-Stabilize
**Est**: ≤30 min
**Depends on**: M0-002 PR merged, M0-003 complete

## TASK
After main has the stabilized WIP merged, confirm the full local validation suite passes. Fix small breakages inline. For anything > 30 min to fix, open a follow-up issue and skip.

## CONTEXT
- Stabilized main = M0-002 PR merged + M0-003 stashes/discards done
- Required green checks (per `docs/launch-cutover-plan.md` Phase 6):
  ```
  npm run lint
  npm run typecheck
  npm run test
  npm run build
  npm run cf:build
  ```
- `npm run test:e2e` may be slow/flaky; run last and treat failures as soft (ticket, don't block)

## CONSTRAINTS
- Inline fixes only for ≤5 file, ≤30 line changes
- Anything bigger → create new GitHub issue with `gh issue create` and tag in the same milestone
- Banned-term scan: `grep -RniE "humanizer|bypass|undetect|detector|evade" app components public lib` must remain empty
- No secrets in any commit
- No deploys

## CHANGES REQUIRED
1. From main (post-merge): `git checkout main && git pull`
2. Run each command in order. For each failure:
   - If trivial (typo, missing import, stale snapshot): fix inline
   - If non-trivial: `gh issue create --milestone M0-Stabilize --title "M0-followup: <description>" --body "Discovered during M0-004. <details>"`
3. If inline fixes made, commit on `chore/m0-green-baseline` branch and open PR
4. Run banned-term scan; clean output required
5. Write `plans/m0-green-baseline-report.md` recording: commands run, results, fixes applied, followup issue numbers

## ACCEPTANCE
- All five commands (lint/typecheck/test/build/cf:build) exit 0 on current main
- Banned-term scan empty
- Any deferred fixes have GitHub issue numbers
- Report written

## DO NOT
- Skip lint or typecheck failures
- Mass-refactor to fix tests
- Touch `.env.local`, Stripe config, or `LAUNCH_CONFIRMED`
- Push directly to main; use branch + PR

## On completion
Comment with: which commands passed, what was fixed inline, what was deferred to follow-up issues. Close the issue when all 5 commands are green on main.
