# Issue M1-009

Title: M1-009 Add Entra token validation tests
Milestone: M1-Entra

## Task

Add unit coverage for Entra token validation behavior using a mock JWKS endpoint.

## Scope

- Touch only `tests/unit/entra-auth.test.ts` unless a tiny test helper adjustment is required.
- Cover valid token to user, expired token to 401, wrong audience to 401, wrong issuer to 401, missing scope to 403, and malformed JWT to 401.
- Do not call live Entra, Azure, or external identity services.

## Constraints

- Do not modify `.env.local`, `.dev.vars`, secrets, live provider settings, or Stripe configuration.
- Follow the current banned-positioning list from `plans/codex-implementation-prompt.md`; do not quote the blocked words in new copy.

## Verification

- `npm run lint`
- `npm run typecheck`
- `npm run test`
