# M0-002: Commit kept WIP in themed branches

**Milestone**: M0-Stabilize
**Est**: ≤30 min
**Depends on**: M0-001 (wip-triage.md must exist)

## TASK
Per `plans/wip-triage.md`'s "Keep" section, create themed commits on a new branch `chore/m0-stabilize-wip`, push the branch, open a PR for human review.

## CONTEXT
- Triage report at `plans/wip-triage.md` lists files grouped by theme
- Goal: convert messy 52-file dirty tree → reviewable PR with logical commits
- Do NOT merge to main — human reviews

## CONSTRAINTS
- Banned-term scan: `grep -RniE "humanizer|bypass|undetect|detector|evade" app components public lib` must remain empty for committed content
- No secrets in any commit message or content
- Commit messages: conventional commits format (`feat:`, `fix:`, `chore:`, `docs:`, `test:`)
- Scope cap: if more than 30 minutes, stop and report

## CHANGES REQUIRED
1. Create branch: `git checkout -b chore/m0-stabilize-wip`
2. For each theme in `wip-triage.md` "Keep" section:
   - `git add` the files in that theme
   - `git commit -m "<type>: <theme summary>"` with bullet body if needed
3. Push branch: `git push -u origin chore/m0-stabilize-wip`
4. Open PR via `gh pr create --base main --head chore/m0-stabilize-wip --title "M0-002: Stabilize dirty WIP" --body "Closes #<this issue> via supervised batch. See plans/wip-triage.md for triage decisions."`
5. Files marked "discard" / "stash" are NOT touched in this issue (handled in M0-003)

## ACCEPTANCE
- Branch `chore/m0-stabilize-wip` exists on remote
- Each kept theme is a single commit (or close to it)
- PR is open against main
- "Discard" and "stash" files still appear in `git status` on the branch — they're handled by M0-003
- Working tree on the branch is otherwise clean

## DO NOT
- Merge the PR yourself
- Push to `main` directly
- Force-push
- Include any "discard" or "stash" files
- Run `wrangler deploy` or modify `LAUNCH_CONFIRMED`

## On completion
Comment with the PR URL and commit list. Leave issue open until PR merges; then close.
