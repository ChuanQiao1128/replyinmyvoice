# M0-006: Update AGENTS.md to reflect Stripe live state

**Milestone**: M0-Stabilize
**Est**: ≤30 min
**Depends on**: M0-004 (clean working tree)

## TASK
AGENTS.md currently says "Keep Stripe in sandbox mode" — that rule is stale. The user has cut over to live (`.env.local` has `sk_live_*`, `LAUNCH_CONFIRMED=true`, `STRIPE_LIVE_CUTOVER_APPROVED=true`). Update AGENTS.md to reflect the new reality while preserving all other supervisor / banned-term / secret rules.

## CONTEXT
- File: `/Users/qc/Desktop/CloudFlare/AGENTS.md` (~1900 lines)
- Stripe section currently mandates sandbox; needs replacement
- Launch cutover rule (`LAUNCH_CONFIRMED=true` required) is now SATISFIED, but the rule itself should remain documented for future toggles
- Do NOT remove or weaken: banned-term rules, secrets policy, supervisor flow, deploy gates
- Do NOT change CLAUDE.md (mirrors AGENTS.md; will reflect via the "AGENTS.md is source of truth" pointer)

## CONSTRAINTS
- Preserve every section heading and the rule structure; this is a targeted update, not a rewrite
- Add a dated "Change log" entry at top noting Stripe sandbox → live cutover on 2026-05-21
- Banned-term scan must still pass
- No secrets in the file
- Single PR

## CHANGES REQUIRED
1. New branch: `chore/m0-agents-md-live-mode`
2. In AGENTS.md, find the Stripe section. Replace "Keep Stripe in sandbox mode" with:
   ```
   ## Stripe Mode

   As of 2026-05-21, Stripe is in LIVE mode. The `STRIPE_LIVE_CUTOVER_APPROVED=true` flag in `.env.local` records the user's explicit go-ahead.

   Local development may use sandbox keys for testing, but production runtime uses live keys via `wrangler secret` on the `replyinmyvoice-app` Worker.

   Codex MUST NOT:
   - Initiate test charges against live keys
   - Modify `STRIPE_PRICE_ID` without the user's explicit instruction in the brief
   - Modify `STRIPE_WEBHOOK_SECRET`
   - Run `stripe trigger` against the live webhook endpoint
   ```
3. In the "Launch cutover" section: update status to note `LAUNCH_CONFIRMED=true` is in effect; preserve the rule that flipping it requires explicit user action
4. Add a "Change log" at the top (or under a "## Recent Decisions" section if one exists):
   ```
   ### 2026-05-21
   - Stripe sandbox → live cutover. `STRIPE_LIVE_CUTOVER_APPROVED=true` in `.env.local`. AGENTS.md updated to reflect live mode.
   - `LAUNCH_CONFIRMED=true` set. `replyinmyvoice.com` formal domain cutover authorized.
   ```
5. Verify banned-term scan still passes
6. Open PR

## ACCEPTANCE
- AGENTS.md no longer says "Keep Stripe in sandbox mode"
- New Stripe Mode section reflects live with codex constraints
- Change log records the date and the flags
- All other rules intact (banned terms, secrets, supervisor flow, deploy gates)
- Banned-term scan clean
- PR open

## DO NOT
- Rewrite or condense unrelated sections
- Remove rules about deploys, secrets, banned terms
- Update CLAUDE.md (it'll inherit via the pointer)
- Touch `.env.local`

## On completion
Comment with PR URL + section-level diff summary. Close when PR merges.
