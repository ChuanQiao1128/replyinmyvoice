# Issue M3-002

Title: M3-002 Reduce visible tone presets to 4
Milestone: M3-V2

## Task

Reduce `tonePresetOptions` to Warm, Professional, Friendly, and Concise.

## Scope

- Touch only `lib/rewrite-presets.ts` and `tests/unit/rewrite-presets.test.ts`.
- Preserve `tonePresetToTone` compatibility mapping for legacy tone ids.
- Update tests to assert the visible preset list and the compatibility mapping.

## Constraints

- Do not modify `.env.local`, `.dev.vars`, secrets, live provider settings, or Stripe configuration.
- Follow the current banned-positioning list from `plans/codex-implementation-prompt.md`; do not quote the blocked words in new copy.

## Verification

- `npm run lint`
- `npm run typecheck`
- `npm run test`
