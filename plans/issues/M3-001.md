# Issue M3-001

Title: M3-001 Add 5 scenarios to lib/rewrite-presets.ts
Milestone: M3-V2

## Task

Define `scenarioOptions: ScenarioOption[]` with exactly: Blank, Email, Customer support, Cover letter, Work update.

## Scope

- Touch only `lib/rewrite-presets.ts` and `tests/unit/rewrite-presets.test.ts`.
- Each scenario option needs an id, label, and helper text.
- Keep this issue additive; do not wire scenario behavior into API or UI here.

## Constraints

- Do not modify `.env.local`, `.dev.vars`, secrets, live provider settings, or Stripe configuration.
- Follow the current banned-positioning list from `plans/codex-implementation-prompt.md`; do not quote the blocked words in new copy.

## Verification

- `npm run lint`
- `npm run typecheck`
- `npm run test`
