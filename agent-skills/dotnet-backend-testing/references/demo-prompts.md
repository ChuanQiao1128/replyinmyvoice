Use these prompts to demonstrate this skill without touching production systems.

```text
Use dotnet-backend-testing to add tests for quota reservation finalization when the provider succeeds after a retry.
```

```text
Use dotnet-backend-testing to review whether the ASP.NET Core rewrite API tests cover validation, idempotency, quota, and persisted state.
```

```text
Use dotnet-backend-testing to design xUnit and WebApplicationFactory tests for Stripe webhook replay and subscription entitlement updates.
```
